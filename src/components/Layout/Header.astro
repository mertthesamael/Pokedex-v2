---
interface Props {
    title: string;
}

const { title } = Astro.props;
---

<header class="w-full h-[--header-height] bg-white shadow-lg fixed">
    <div class="w-full h-full flex justify-between items-center p-8">
        <div>search</div>
        <div>
            <h1 class="text-2xl">{title}</h1>
        </div>
    </div>
</header>

